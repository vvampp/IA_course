#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

const int WIDTH = 1200, HEIGHT = 1000;
const int BOARD_SIZE = 3;
const int SQUARE_SIZE = 200;
const float LINE_THICKNESS = 6.0f;
const float BOARD_LENGHT = BOARD_SIZE * SQUARE_SIZE;
const float PADDING_X = (WIDTH-BOARD_LENGHT)/2;
const float PADDING_Y = (HEIGHT-BOARD_LENGHT)/2;

#endif

